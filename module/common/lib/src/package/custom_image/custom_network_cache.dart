/// Custom mem cache for using network image
final class CustomNetworkCache {
  /// [height] and [width] is used for caching image
  const CustomNetworkCache({required this.height, required this.width});
  final int height;
  final int width;
}
